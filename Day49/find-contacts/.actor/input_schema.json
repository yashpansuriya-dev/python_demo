{
  "$schema": "https://apify.com/schemas/v1/input.ide.json",
  "title": "Find Contacts",
  "description": "Extract emails, phones, addresses, LinkedIn and other social media links from a list of websites.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "start_urls": {
      "title": "Website URLs",
      "description": "Websites to inspect. The actor will also visit likely contact/about/location pages on each domain.",
      "type": "array",
      "editor": "requestListSources",
      "prefill": [
        { "url": "https://apify.com" }
      ]
    },
    "max_pages_per_site": {
      "title": "Max Pages Per Site",
      "description": "Maximum number of internal pages to open for each website.",
      "type": "integer",
      "default": 5,
      "minimum": 1,
      "maximum": 20,
      "editor": "number"
    }
  },
  "required": ["start_urls"]
}
